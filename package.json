{
  "name": "audio-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync:assets": "node scripts/sync_browser_assets.mjs",
    "export:eou-onnx": "python3 scripts/export_baseline_eou_onnx.py",
    "prepare:browser": "npm run sync:assets && npm run export:eou-onnx",
    "test": "node --test --experimental-strip-types tests/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "@ricky0123/vad-web": "^0.0.30",
    "onnxruntime-web": "^1.24.3",
    "vosk-browser": "^0.0.8"
  }
}
